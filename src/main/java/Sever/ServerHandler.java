package Sever;

import io.netty.channel.ChannelHandler;
import io.netty.util.concurrent.EventExecutorGroup;

public class ServerHandler implements ChannelHandler, EventExecutorGroup {
    public ServerHandler(Object login) {
    }
}
